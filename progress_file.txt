# 進度追蹤文件

## 目前狀態：待機中 ✅（2026-05-13）— fullPageInteraction 修復完成，APK 已建置（commit 9a6842a）

最後完成任務：fullPageInteraction 修復：表單填寫步驟（S4 帳戶表單、S5 支出表單）不再有黑色遮罩阻擋，使用者可自由操作表單 ✅

### 本次完成（2026-05-13，commit 9a6842a）
- [x] **fullPageInteraction 修復**（tour 遮罩完全不阻擋表單）：
  - **`tour_overlay.dart`**：`isActionRequired && spotRect == null` 時跳過黑色遮罩層和所有阻擋器，只顯示提示面板（panel hint），面板外所有事件穿透到底下頁面
  - **`tour_controller.dart`**：
    - `notifyAddAccountPageOpened()` 現在也清除 `_cachedTargetRect` + `notifyListeners()`，讓 AddEditAccountPage 一推送就進入 fullPageInteraction
    - 新增 `notifyFormOpened()`：清除 spotlight rect 並通知（用於 FAB → 表單頁面）
    - 新增 `notifyFormDismissed()`：使用者取消表單時重新跑 `_prepareStep()` 恢復 spotlight
  - **`main.dart`**：`_fabTap()` 改呼叫 `notifyFormOpened()`（取代 `notifyRouteOpened`）；`_openAdd()` / `_openAddInvestment()` 取消時呼叫 `notifyFormDismissed()`
  - **`account_page.dart`**：`_openAdd()` 取消時呼叫 `notifyFormDismissed()` 恢復 ＋ 按鈕 spotlight

### 上上次完成（2026-05-13，commit 17c8f63）
- [x] **導覽自動導航 + Spotlight 跨頁面 + 回呼架構（commit 17c8f63）**：
  - **架構升級**：`TourOverlay` 從 `MainShell` Stack 移至 `MaterialApp.builder`，覆蓋整個 Navigator → spotlight 現在可顯示於所有推送頁面（AccountPage、AddEditAccountPage 等）
  - **`tour_session.dart`** 新增欄位：`addAccountPageOpened`、`accountCreated`、`transactionCreated`
  - **`tour_keys.dart`** 新增：`TourKeys.accountAddBtn`（AccountPage 右上角 ＋ 按鈕）
  - **`tour_controller.dart`** 新增：
    - `_popToMain`、`_showSuccessMsg` 欄位（由 main.dart 注入）
    - `notifyAddAccountPageOpened()`：帳戶頁 ＋ 按鈕被點時設定 session flag
    - `onAccountCreated(account)`：帳戶儲存後設定 flag → 觸發 predicate
    - `onTransactionCreated(expense)`：支出儲存後設定 flag → 觸發 predicate
    - `_handleStepCompletionSideEffects()`：qs_s4/fs_s2 完成後自動 popToMain → goToTab(0) → 成功 snackbar
  - **`tour_step.dart`** QuickStart 從 7 步重建為 8 步（qs_s0~qs_s7）：
    - S0: info（歡迎）
    - S1: actionRequired（前往管理頁，spotlight navManage）
    - S2: actionRequired（點帳戶卡，spotlight accountCard，predicate: accountPageWasOpened）
    - S3: actionRequired（點 ＋ 按鈕，spotlight accountAddBtn，predicate: addAccountPageOpened）
    - S4: actionRequired（填表儲存，無 spotlight，predicate: accountCreated → 完成後自動回首頁）
    - S5: actionRequired（記錄支出，spotlight FAB，predicate: transactionCreated callback）
    - S6: info（同步結果，spotlight monthCard，動態內文）
    - S7: finish
  - **`tour_overlay.dart`** 修復互動阻擋邏輯：actionRequired + 無 spotlight = 不阻擋（讓使用者填寫表單）
  - **`account_page.dart`** 更新：
    - ＋ AppBar 按鈕加上 `TourKeys.accountAddBtn` GlobalKey
    - `_openAdd()`：點擊時呼叫 `notifyAddAccountPageOpened()`；儲存後呼叫 `onAccountCreated(result)`
  - **`main.dart`** 更新：
    - `MaterialApp.builder` 放置 TourOverlay（全域覆蓋）
    - `_initTour()` 注入 `popToMain` + `onShowSuccess` 回呼
    - `_openAdd()` 支出儲存後呼叫 `onTransactionCreated(result)`
    - 移除 MainShell Stack 中的 TourOverlay
  - **`localization.dart`** 更新（4 語言）：
    - qs_s2/s3 文案更新（開啟帳戶管理 / 點 ＋ 新增帳戶）
    - 新增 `tour_qs_s4_save_title/body/action_hint`（填寫帳戶資料）
    - 新增 `tour_account_created_success`（成功 snackbar 文案）
  - APK：`build/app/outputs/flutter-apk/app-release.apk`（62.0MB）
  - `flutter analyze`：0 errors，55 items 均為 pre-existing info/warning

### 下一步（驗收測試）
A. 帳戶建立後 → 成功提示 + 自動回 tab 0 + spotlight FAB ✓
B. 取消帳戶建立 → 停在「建立帳戶」步驟 ✓
C. 取消支出表單 → 停在「記錄支出」步驟 ✓
D. 支出實際儲存 → 成功 → 自動回首頁 → 同步可見 ✓
E. 三模式完全獨立（qs/fs/demo 各自流程）✓
F. Spotlight 在每次 tab/路由切換後位置正確 ✓

### 本次完成（2026-05-12，本次 commit）
- [x] **任務型新手導覽狀態機**：
  - **`tour_session.dart`**（新建）：`TourSession`（startAccountCount + createdAt + accountPageWasOpened）
  - **`tour_step.dart`** 全面改寫：
    - `TourStepType` enum：info / actionRequired / finish（取代 TourActionType）
    - `StepPredicate = bool Function(AppState, TourSession, int currentTab)` typedef
    - `DynamicBodyBuilder = String Function(AppState, TourSession, BuildContext)` typedef
    - `TourStep` 新增 `completionPredicate`、`actionHintLocKey`、`dynamicBody` 欄位，移除 `const`
    - `buildQuickStartSteps`：7步全部使用 actionRequired + 真實 predicate（tab/accountCount/expenseCount），S5 動態內文（有無關聯帳戶）
    - `buildFullSetupSteps`：11步使用相同 predicate 架構
    - `buildDemoSteps`：5步，demo_s3 改用 actionRequired（tab==1 predicate）
  - **`tour_controller.dart`** 全面改寫：
    - `AppState? _appState` + `TourSession? _session` + `int _currentTab`
    - `init()` 新增 `required AppState appState` 參數，呼叫 `addListener(_onAppStateChanged)`
    - `startMission()` 建立 TourSession（記錄 startAccountCount）
    - `_onAppStateChanged()` → `_checkCurrentStepPredicate()`
    - `notifyTabChanged()` 更新 `_currentTab` → 觸發 predicate 檢查
    - `notifyRouteOpened()` 設定 `session.accountPageWasOpened` → 觸發 predicate 檢查
    - `next()` 加保護：actionRequired 且 predicate 未滿足時禁止前進
    - `_onActionCompleted()`：predicate 滿足 → 600ms ✓ 閃爍 → `next()`
    - **Spotlight 穩定性修復**：5次重試，比較連續兩次 rect 差距 < 2px 才採用（不再 pre-render 錯誤位置）
    - `dispose()` 正確移除 AppState listener
  - **`tour_overlay.dart`** 更新：
    - 內嵌 `Consumer<AppState>` 取得 dynamicBody 資料
    - `bodyText` 由 `step.dynamicBody!()` 或 `step.body` 決定
    - `_MissionPanel` 新增 `bodyText`、`isCurrentStepCompleted` 參數
    - actionRequired 步驟：右側顯示 step-specific 操作提示（`actionHintLocKey`），無「略過此步」按鈕
    - info/finish 步驟：正常「下一步」/「完成」按鈕
    - 互動阻擋條件改為 `step.isActionRequired`（取代 `step.isInteractive`）
  - **`main.dart`** 更新：`init()` 新增 `appState: context.read<AppState>()`；`startMission()` 移除 hasAccounts/hasTransactions 參數
  - **`localization.dart`** 新增 60 個 key（4語言 × 15 key）：
    - qs/fs/demo 各步驟 `_action_hint` key（actionRequired 步驟等待時顯示）
    - `qs_s5_body_linked`、`qs_s5_body_unlinked`（含 `{amount}` placeholder）
    - `fs_s5_body_linked`、`fs_s5_body_unlinked`
  - APK：`build/app/outputs/flutter-apk/app-release.apk`（62.0MB，需 `--no-tree-shake-icons`）
  - `flutter analyze`：0 errors，54 items 均為 pre-existing info/warning

### 本次完成（2026-05-12，舊 commit）
- [x] **導覽系統三模式真正分流**：
  - **OnboardingMode enum**（tour_step.dart）：quickStart / fullSetup / demo
  - **三套獨立 step builder**：
    - `buildQuickStartSteps`：7步（qs_s0~qs_s6）帳戶 + 第一筆支出閉環
    - `buildFullSetupSteps`：11步（fs_s0~fs_s10）完整帳戶 + 記帳 + 預算 + 明細 + 備份
    - `buildDemoSteps`：5步（demo_s0~demo_s4）純說明，使用示範資料
  - **OnboardingStartPage**：改為 `void Function(OnboardingMode) onSelect`，移除 nullable fallback
  - **main.dart**：`_launchMission(appState, mode)` 接收 mode；demo 進入前呼叫 `appState.loadDemoData()`；`_goToTabAsync` 供 Tour 使用（await animateToPage）；Demo exit dialog（清除/保留選項）
  - **TourController**：`startMission(context, mode, ...)` 接收 mode；`_goToTab` 改為 `Future<void> Function`；await 完成頁面切換動畫後才計算 spotlight rect；retry 從 2 次增加到 3 次；`clearDemoMode()` 方法；不再自動呼叫 `markOnboardingSeen`（由 main.dart 的 `_handleTourEnd` 負責）
  - **Demo 資料隔離**：注入 in-memory overlay，不寫入 SharedPreferences；離開時顯示 dialog 讓使用者選擇清除或保留
  - **Demo 橫幅**：tour_overlay.dart 顯示橙色「示範模式」橫幅
  - **log 輸出**：`[Onboarding] start mode=quickStart/fullSetup/demo`
  - **四語系**：zh_TW/zh_CN/en_US/ja_JP 新增 qs/fs/demo 三套共 54 個 key + banner + exit dialog key
  - APK：`build/app/outputs/flutter-apk/app-release.apk`（61.9MB）

### 本次完成（2026-05-12，commit 914aec7）
- [x] **新手導覽升級為「財務初始化任務」系統**：
  - **架構重構**：
    - `tour_step.dart`：新增 `finish` 動作類型，`buildMissionSteps(context, {hasAccounts, hasTransactions})` 取代舊固定 16 步，改為 13 步資料狀態分支任務
    - `tour_controller.dart`：`startMission()` 接受資料狀態，`_stepJustCompleted` 互動完成回饋，`_onActionCompleted()` 延遲 600ms 再推進
    - `tour_overlay.dart`：互動步驟完成顯示 ✓ 閃爍回饋，finish 步驟顯示「完成 ✓」按鈕
  - **新入口畫面**：`onboarding_start_page.dart`（新建）三選項卡片（快速開始/完整設定/先看範例）+ 跳過
  - **main.dart 接入**：`_checkOnboarding` → `_showOnboardingStart`，`_launchMission()` 傳即時 hasAccounts/hasTransactions
  - **資料狀態分支**：
    - 無帳戶：S1「建立帳戶」，S7 警告 + 允許略過
    - 無交易：S8/S9 空狀態文案，不強說「長按記錄」
    - 有資料：S11 強調備份，S9 顯示操作說明
  - **四語系更新**：zh_TW/zh_CN/en_US/ja_JP 全部替換 tour_s0~s15 為新 s0~s12 + 變體文案 + onboarding_start_* 字串
- APK：`build/app/outputs/flutter-apk/app-release.apk`（61.8MB）

### 本次完成（2026-05-12，commit f2c3d06）
- [x] **導覽 spotlight 三項修復**：
  - **Step 10/14 nav item 偏左修復**：`_NavItem` 加 `tourKey` param，把 GlobalKey 放在 `SizedBox.expand`（SingleChildRenderObjectWidget）上，`findRenderObject()` 直接取得正確 RenderBox，消除偏左一格的問題
  - **Step 15 backupCard 截斷修復**：`tour_overlay.dart` clip 閾值從 0.4→0.65，clip 高度從 80→250dp，備份卡（~55% 螢幕高）不再被截斷
  - **Step 3 帳戶創建引導**：`AccountPage` 加 `isFromTour` param，從導覽開啟時頂部顯示金色提示 bar（「點右上角 ＋ 新增帳戶」），S2/S3 說明文字四語言更新
- APK：`build/app/outputs/flutter-apk/app-release.apk`（61.8MB）

### 本次完成（2026-05-12，commit 86e6112）
- [x] **新手導覽任務式大改版**：
  - 架構重寫：TourActionType（infoOnly / waitForTabChange / waitForRouteOpen）
  - TourController 改為事件驅動：notifyTabChanged() / notifyRouteOpened()
  - TourOverlay 改為底部 mission panel，spotlight 清楚置中顯示
  - 16 步任務流程：使用者必須實際點 tab、帳戶卡、FAB 才能進下一步
  - 點錯位置顯示 ⚠️ 警示（notifyWrongTap），「略過此步」避免卡死
  - panel 智慧判斷頂/底部位置（spotlight 在下方時 panel 移到頂部）
  - 四語言（zh_TW / zh_CN / en_US / ja_JP）全面更新為任務語氣文案
  - APK：`build/app/outputs/flutter-apk/app-release.apk`（61.8MB）

### 本次完成（2026-05-12，commit c75ebd7）
- [x] **導覽 interactive steps 移除**：
  - S6（新增支出 FAB）、S8（長按刪除）、S10（新增持股 FAB）移除 `isInteractive:true`
  - 修復：action sheet 與 tour overlay 同時顯示、用戶完全卡住無法前進
  - 三步改為純說明步驟，有「下一步」按鈕，流程順暢
- [x] **spotlight 可視性加強**：
  - 新增白色實線邊框（alpha 0.55），spotlight 切口在真機上清晰可見
  - glow ring: sigma 4→8, alpha 0.20/0.60→0.35/0.90, strokeWidth 2.5→3.5
- APK：`build/app/outputs/flutter-apk/app-release.apk`（61.8MB）

### 本次完成（2026-05-12，commit 2d94788）
- [x] **新手導覽位置跳動修復**：
  - 根本原因：overlay 在 scroll+layout 結束前就讀取 RenderBox 位置，讀到 PageView 相鄰頁的 stale 座標
  - `tour_controller.dart`：新增 `_cachedTargetRect`，在 tab 切換 → scroll → `endOfFrame` 全部完成後才計算 rect
  - 顯示流程改為：`_hidden = true` → `_prepareStep()` → `_cachedTargetRect` 計算完 → `_hidden = false` → `notifyListeners()`
  - 新增 `_findRectOnScreen()`：加 ±600dp sanity check，過濾掉 PageView 相鄰頁的 off-screen 座標
  - 最多重試 2 個 endOfFrame，適應 lazy builder 的延遲 layout
  - `tour_overlay.dart`：改讀 `ctrl.cachedTargetRect`（不再在 build() 裡呼叫 `_findRect()`）
  - spotlight rect 加 screen bounds clamp，防止光圈超出螢幕邊界
  - tooltip 位置邏輯改進：考慮 roomBelow/roomAbove 雙側空間，無空間時置中
  - APK：`build/app/outputs/flutter-apk/app-release.apk`（61.8MB）

### 本次完成（2026-05-11，commit 6f757fa）
- [x] **MP-01 新手導覽效能修復**：
  - 移除 `BackdropFilter(sigmaX:24)` — 消除 GPU framebuffer readback（最大效能瓶頸）
  - `_SpotlightPainter`：`saveLayer + BlendMode.clear` → `Path.evenOdd`（消除 offscreen buffer）
  - glow blur sigma：10 → 4（GPU 負擔降低約 6 倍）
  - `RepaintBoundary` 包住 `AnimatedBuilder`，隔離 60fps 重繪區域
  - `next()`/`prev()` 加 `_transitioning` lock，防止快速連點 race condition
  - `notifyListeners()` 改在 `_prepareStep()` 之前呼叫 → 點下一步立即有視覺回應
  - `_prepareStep` 等待：420ms+120ms → 150ms+60ms（步驟切換快一倍）
  - APK：`build/app/outputs/flutter-apk/app-release.apk`（61.8MB）

### 本次完成（2026-05-11，commit 46ca234）
- [x] **QA MAX 審計 + P0/P1 修復**：
  - P0：fixedTotal 雙重計算（已執行固定開銷不再重複加入預算）
  - P1：投資損益不一致（totalPortfolioProfit 改用 net，與個別持股一致）
  - P2 待修：FX備份、借款利息收入、跨幣別轉帳、clearAll debt account
  - 7 項需人工真機確認（見報告）

### 本次完成（2026-05-11，commit 1ad9991）
- [x] **新手導覽重設計**：
  - 新流程：帳戶（基礎）→ 儀表板 → 預算 → 記帳 → 明細 → 投資 → 備份
  - tour_step.dart：15步縮減為13步（S0-S12），移除 feedback 互動 + investRefresh
  - tour_keys.dart：移除廢棄的 fixedCard GlobalKey
  - localization.dart：四語言 tour 文案全面改寫，對齊設定引導邏輯

### 本次完成（2026-05-11，commit db6782c）
- [x] **QA Audit 完成**：Phase 0-11 完整掃描，P0=0，P1→已修，P2→已修
- [x] **P1 修復**：amount_calculator_sheet 硬編碼中文「請輸入有效金額」→ AppLocalizations
- [x] **P2 修復**：
  - backup_service 版本相容性改為支援 1.0/2.0/3.0
  - 移除 unused import loan_page.dart + 死函式 _openFixedDialog
  - clearAll 後 showSnackBar 加 mounted 保護
  - activeColor → activeThumbColor（4 個檔案）

### 本次完成（2026-05-10，commit 573321c）
- [x] **資產頁「含股票帳戶」開關**

### 里程碑（2026-05-10）
- [x] Google Play Console 身分驗證 ✅
- [x] Google Play Console 電話驗證 ✅
- [ ] 在 Play Console 建立 App（填套件名稱 / App 名稱 / 類別）
- [ ] 上傳 AAB v2.0.0+2
- [ ] 填寫商店資訊
- [ ] 送審

### 下一步
1. 安裝 APK 真機測試導覽流程（確認位置跳動已消除）
2. Sean 在 Play Console 建立 App → 上傳 AAB → 送審
