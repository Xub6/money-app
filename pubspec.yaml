name: money_app
description: "錢錢管家 - 個人記帳 App"
publish_to: 'none'
version: 2.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.3.3
  intl: ^0.19.0

  # Phase 1 & 2: Core dependencies
  uuid: ^4.0.0                      # Secure ID generation
  sqflite: ^2.3.0                   # SQLite database
  path_provider: ^2.1.0             # File paths
  csv: ^6.0.0                       # CSV export
  excel: ^3.0.0                     # Excel export

  # Phase 2: Security & Storage
  flutter_secure_storage: ^9.0.0    # Encrypted storage
  encrypt: ^5.0.3                   # AES encryption (null-safe)
  provider: ^6.0.0                  # State management upgrade
  path: ^1.9.0                      # Path utilities
  http: ^1.2.0                      # HTTP requests (Yahoo Finance)
  crypto: ^3.0.0                    # SHA256 hashing

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/icon.png
    - assets/icon.jpg
